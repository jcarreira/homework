Scripts to launch the experiments are in the scripts folder.

sh scripts/get_expert_data.sh # get observations/actions from experts and saves them in a file

sh scripts/get_scores_from_bc.sh # learns a BC policy based on the data generated by the previous step

sh scripts/get_scores_from_dagger.sh # learns a policy with Dagger based on the experts output

sh run_all_experts.sh # runs all experts (used to check their mean returns and stds)


